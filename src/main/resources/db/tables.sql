CREATE  TABLE `batch`.`database1` (
  `id` INT NOT NULL ,
  `status` VARCHAR(45) NULL ,
  PRIMARY KEY (`id`) );

CREATE  TABLE `batch`.`database2` (
  `id` INT NOT NULL ,
  `status` VARCHAR(45) NULL ,
  PRIMARY KEY (`id`) );
